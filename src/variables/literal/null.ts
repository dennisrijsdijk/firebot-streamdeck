import {ReplaceVariable} from "../../types/replaceVariable";

const model: ReplaceVariable = {
    handle: "null",
    usages: [
        {
            handle: "null",
            description: "Get a null literal"
        }
    ],
    evaluator: async () => null
}

export default model;